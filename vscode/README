===============================================================================
                               VSCODE SETUP
===============================================================================

-------------------------------------------------------------------------------
Install extensions from a list
-------------------------------------------------------------------------------

xargs -n 1 code --install-extension < vscode_extensions.txt

-------------------------------------------------------------------------------
Link VSCode folder from repo root
-------------------------------------------------------------------------------

ln -s "$(pwd)/vscode" "/Users/you/linked-folder/.vscode"

-------------------------------------------------------------------------------
Test this repo locally
-------------------------------------------------------------------------------

ln -s "$(pwd)/vscode" "$(pwd)/.vscode"

-------------------------------------------------------------------------------
Bind keybindings on MacOS
-------------------------------------------------------------------------------

ln -s "$(pwd)/vscode/keybindings.json" \
      "$HOME/Library/Application Support/Code/User/keybindings.json"

-------------------------------------------------------------------------------
Bind general appearance settings on MacOS
-------------------------------------------------------------------------------

ln -s "$(pwd)/vscode/settings.json" \
      "$HOME/Library/Application Support/Code/User/settings.json"

===============================================================================
